package com.tagmypet.ui.theme

import androidx.compose.ui.graphics.Color

// --- Tag My Pet Palette (Converted from HSL) ---

// Primary: hsl(16 85% 58%) -> Coral Orange
val Primary600 = Color(0xFFF56C36)
val Primary700 = Color(0xFFD9521E) // Um tom mais escuro para cliques
val Primary100 = Color(0xFFFEF0EA) // Fundo suave laranja

// Backgrounds: hsl(30 50% 98%) -> Warm Cream
val Background = Color(0xFFFFFCF9)
val Surface = Color(0xFFFFFFFF)

// Foreground/Text: hsl(20 25% 15%) -> Dark Warm Brown
val TextPrimary = Color(0xFF30251F)
val TextSecondary = Color(0xFF6E645F) // Muted foreground

// Functional
val Destructive = Color(0xFFEF4444) // Vermelho padrão
val DestructiveBg = Color(0xFFFEF2F2)
val PawLove = Color(0xFFE8485B) // hsl(350 80% 60%) -> Pinkish Red

// Borders & Dividers: hsl(30 20% 88%)
val BorderColor = Color(0xFFE6E1DC)
val InputBg = Color(0xFFF7F5F3) // Levemente mais escuro que o bg

// Gradients (Brush support requires more code in UI, keeping colors here)
val GradientStart = Color(0xFFF56C36)
val GradientEnd = Color(0xFFFDBA74) // Laranja mais claro